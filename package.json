{
  "name": "dot-x-plugins-registry",
  "version": "1.0.0",
  "description": "Plugin marketplace registry infrastructure",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}

